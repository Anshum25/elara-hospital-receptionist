"""
backend/__init__.py

WHY this file exists:
Python uses __init__.py to mark a directory as a "package" (a collection of modules).
Without this file, Python wouldn't let you do: "from backend.rag_pipeline import ..."

This is like putting a name tag on a folder so Python knows it contains importable code.
"""
